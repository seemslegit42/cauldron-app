import { GroqInferenceInput } from '../ai-services/groq';

// Extend the Wasp server operations with our custom types
declare module 'wasp/server/operations' {
  export type GroqInference<Input extends GroqInferenceInput, Output> = (args: Input, context: any) => Promise<Output>;
}

// Extend the Wasp client operations with our custom types
declare module 'wasp/client/operations' {
  export const groqInference: <Input extends GroqInferenceInput, Output>(args: Input) => Promise<Output>;
}import { GroqInferenceInput } from '../ai-services/groq';

// Extend the Wasp server operations with our custom types
declare module 'wasp/server/operations' {
  export type GroqInference<Input extends GroqInferenceInput, Output> = (args: Input, context: any) => Promise<Output>;
}

// Extend the Wasp client operations with our custom types
declare module 'wasp/client/operations' {
  export const groqInference: <Input extends GroqInferenceInput, Output>(args: Input) => Promise<Output>;
}import { GroqInferenceInput } from '../ai-services/groq';

// Extend the Wasp server operations with our custom types
declare module 'wasp/server/operations' {
  export type GroqInference<Input extends GroqInferenceInput, Output> = (args: Input, context: any) => Promise<Output>;
}

// Extend the Wasp client operations with our custom types
declare module 'wasp/client/operations' {
  export const groqInference: <Input extends GroqInferenceInput, Output>(args: Input) => Promise<Output>;
}import { GroqInferenceInput } from '../ai-services/groq';

// Extend the Wasp server operations with our custom types
declare module 'wasp/server/operations' {
  export type GroqInference<Input extends GroqInferenceInput, Output> = (args: Input, context: any) => Promise<Output>;
}

// Extend the Wasp client operations with our custom types
declare module 'wasp/client/operations' {
  export const groqInference: <Input extends GroqInferenceInput, Output>(args: Input) => Promise<Output>;
}import { GroqInferenceInput } from '../ai-services/groq';

// Extend the Wasp server operations with our custom types
declare module 'wasp/server/operations' {
  export type GroqInference<Input extends GroqInferenceInput, Output> = (args: Input, context: any) => Promise<Output>;
}

// Extend the Wasp client operations with our custom types
declare module 'wasp/client/operations' {
  export const groqInference: <Input extends GroqInferenceInput, Output>(args: Input) => Promise<Output>;
}import { GroqInferenceInput } from '../ai-services/groq';

// Extend the Wasp server operations with our custom types
declare module 'wasp/server/operations' {
  export type GroqInference<Input extends GroqInferenceInput, Output> = (args: Input, context: any) => Promise<Output>;
}

// Extend the Wasp client operations with our custom types
declare module 'wasp/client/operations' {
  export const groqInference: <Input extends GroqInferenceInput, Output>(args: Input) => Promise<Output>;
}import { GroqInferenceInput } from '../ai-services/groq';

// Extend the Wasp server operations with our custom types
declare module 'wasp/server/operations' {
  export type GroqInference<Input extends GroqInferenceInput, Output> = (args: Input, context: any) => Promise<Output>;
}

// Extend the Wasp client operations with our custom types
declare module 'wasp/client/operations' {
  export const groqInference: <Input extends GroqInferenceInput, Output>(args: Input) => Promise<Output>;
}import { GroqInferenceInput } from '../ai-services/groq';

// Extend the Wasp server operations with our custom types
declare module 'wasp/server/operations' {
  export type GroqInference<Input extends GroqInferenceInput, Output> = (args: Input, context: any) => Promise<Output>;
}

// Extend the Wasp client operations with our custom types
declare module 'wasp/client/operations' {
  export const groqInference: <Input extends GroqInferenceInput, Output>(args: Input) => Promise<Output>;
}import { GroqInferenceInput } from '../ai-services/groq';

// Extend the Wasp server operations with our custom types
declare module 'wasp/server/operations' {
  export type GroqInference<Input extends GroqInferenceInput, Output> = (args: Input, context: any) => Promise<Output>;
}

// Extend the Wasp client operations with our custom types
declare module 'wasp/client/operations' {
  export const groqInference: <Input extends GroqInferenceInput, Output>(args: Input) => Promise<Output>;
}